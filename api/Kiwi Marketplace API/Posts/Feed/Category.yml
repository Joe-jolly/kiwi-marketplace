info:
  name: Category
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/posts?categoryId=furniture
  params:
    - name: categoryId
      value: furniture
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
