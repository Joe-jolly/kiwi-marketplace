info:
  name: Limit
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/posts?limit=3
  params:
    - name: limit
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
